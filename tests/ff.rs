#[allow(unused_variables, unused_assignments, unused_imports)]
use std::io;
fn main() {
  let x = 5;
  let y = 6;
  let z = y + x;
  println!("{}", z);
  println!("{}", x);
  println!("{}", y);
  println!("{}", z);
}
